# Anthropic API key for the legacy direct-SDK path.
# REMOVE after Phase B cleanup once the Lambda path is verified end-to-end.
VITE_ANTHROPIC_API_KEY=

# AWS Cognito + API Gateway config — wired up by Terraform.
# Values below are the live us-east-1 deploy; non-secret (they're shipped in
# the SPA bundle to every authenticated user anyway).
VITE_COGNITO_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=us-east-1_sTipV6GGg
VITE_COGNITO_APP_CLIENT_ID=4pfrckqqi4v6po6i894aehk7km
VITE_API_ENDPOINT=https://9wjouu57ze.execute-api.us-east-1.amazonaws.com
